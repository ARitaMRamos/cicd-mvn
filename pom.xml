<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://.apache.org/POM/4.0.0 http://.apache.org/xsd/-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.celfocus.qa.cicd.training</groupId>
    <artifactId>message-receiver</artifactId>
    <version>0.7.0</version>
    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <.compiler.source>1.8</.compiler.source>
        <.compiler.target>1.8</.compiler.target>
        <junit-jupiter-engine.version>5.7.1</junit-jupiter-engine.version>
        <-surefire-plugin.version>2.22.2</-surefire-plugin.version>
        <jacoco--plugin.version>0.8.6</jacoco--plugin.version>
    </properties>
    <build>
        <finalName>message-receiver</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache..plugins</groupId>
                <artifactId>-surefire-plugin</artifactId>
                <version>${-surefire-plugin.version}</version>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco--plugin</artifactId>
                <version>${jacoco--plugin.version}</version>
                <executions>
                    <execution>
                        <id>jacoco-initialize</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>jacoco-report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <excludes>
                        <exclude>**/Main.class</exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>
    <dependencies>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <version>${junit-jupiter-engine.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
